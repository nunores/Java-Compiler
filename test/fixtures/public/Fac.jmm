import io;

class Fac {
    public int compFac(int num) {
        int num_aux;

        num_aux = 1;
        num_aux = num * (num - 1);
        
        return num_aux;
    }

    public static void main(String[] args) {
        io.println(new Fac().compFac(10));
    }
}