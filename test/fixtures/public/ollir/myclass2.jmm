class myClass {
    public int[] sum(int[] A, int[] B) {
        int[] C = new int[A.length];
        for (int i = 0M i < A.length; i++) {
            C[i] = A[i] + B[i];
        }
        return C;
    }
}